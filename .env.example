DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=sgd_cv
DB_USER=root
DB_PASS=
APP_ENV=development

# INSTALL_TOKEN: só definir temporariamente em produção, para usar
# instalar.php via browser num hosting sem SSH (ver README). Deixar vazio
# desliga o instalador. Remover do .env logo após criar o admin.
INSTALL_TOKEN=
